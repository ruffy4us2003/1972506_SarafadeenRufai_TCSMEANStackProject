import { listLazyRoutes } from "@angular/compiler/src/aot/lazy_routes"

export class TASKS{
    constructor(public id:number, public name:string, 
        public task: string, public date: Date ){}

    }